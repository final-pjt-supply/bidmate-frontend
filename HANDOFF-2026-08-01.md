# 핸드오프 — 2026-08-01 작업용

2026-07-30~31 세션 정리. 다른 PC에서 이어서 작업하기 위한 핸드오프.

---

## 0. 지금 하던 일 (바로 이어서 할 것)

**필수개선 #1: 세션 확인 중 로그아웃 화면 순간 노출** — 계획까지만 세우고 착수 전이다.

- 문제: `src/lib/auth.tsx`의 `ready`가 새로고침마다 `false`로 시작해 Cognito 세션 확인 + `/api/me` 호출이 끝나야 `true`가 된다. `home-view.tsx`/`reco-view.tsx`/`ai-reco-view.tsx` 세 곳 전부 `isMember = ready && !!user`라는 단일 boolean으로 "확인 중"과 "확실히 비회원"을 구분 없이 취급해서, 로그인된 사용자가 새로고침할 때마다 잠깐 "로그인하면 우리 회사 맞춤 공고를 추천해드려요" 화면이 먼저 뜨고 진짜 화면으로 바뀐다.
- 계획: 세 파일 모두 `isMember` 분기보다 먼저 `!ready` 전용 중립 스켈레톤을 넣는다. `reco-view.tsx`/`ai-reco-view.tsx`는 이미 있는 6칸 `animate-pulse` 카드 그리드를 재사용, `home-view.tsx`는 히어로+바디를 `bg-slate-100` 뼈대로 채운다(`mypage-shell.tsx`에서 쓴 것과 같은 톤).
- 사용자에게 계획 보고까지 했고, "이슈생성 브랜치생성 후 진행해줘" 확인만 받으면 된다.

---

## 1. 오늘 머지된 PR (전부 완료, 배포까지 확인함)

| PR | 내용 |
|---|---|
| [#152](https://github.com/final-pjt-supply/bidmate-frontend/pull/152) | 이용약관에 프로젝트 성격 고지 조항 추가(비상업 부트캠프 프로젝트 고지) |
| [#154](https://github.com/final-pjt-supply/bidmate-frontend/pull/154) | 홈 "내 조건 맞춤 추천" 섹션에 실제 매칭 데이터 연결(가짜 개인화 수정) |
| [#156](https://github.com/final-pjt-supply/bidmate-frontend/pull/156) | 푸터 모바일·태블릿 겹침/줄바꿈 문제 수정 |
| [#158](https://github.com/final-pjt-supply/bidmate-frontend/pull/158) | 버그·UI 결함 7건 일괄 수정(아래 3번 참고) |

원격 브랜치는 전부 머지 시 자동 삭제됨. 현재 열린 PR 없음.

**로컬 stray 브랜치 주의**: `codex/exclude-claude-from-docker`(이미 머지된 PR #130, 원격 삭제됨)가 로컬에만 남아있다. 이번 세션에서 만든 게 아니라 지우지 않고 남겨뒀다 — 정리해도 되는지 사용자 확인 필요.

---

## 2. 발견했지만 아직 안 고친 것 (우선순위순)

### 필수개선
1. **세션 확인 중 화면 깜빡임** — 위 0번, 계획 완료·착수 전
2. **회사정보 유무를 localStorage로만 판정** — 다른 기기/시크릿창에서 서버엔 있어도 "입력해주세요"로 오탐. `src/lib/company.ts`의 `hasCompanyProfile`. 서버 기준으로 바꿀지 설계 판단 필요
3. **`/mypage` 등 폼 페이지 반응형 클래스 전무** — `src/app/mypage/page.tsx`(985줄), 인력입력 행 등이 375px에서 입력 불가 수준. 페이지가 커서 범위를 어떻게 자를지 판단 필요
4. **`/bidbot` 완전한 404화 안 됨** — PR #158에서 프리렌더 고착(`force-dynamic`)은 고쳤지만 여전히 200 응답. 진짜 원인은 루트 `src/app/loading.tsx`가 전역 Suspense 경계를 만들어 스트리밍 헤더가 200으로 먼저 확정되는 것. 고치려면 `loading.tsx` 적용 범위를 라우트 그룹으로 좁히는 구조 변경이 필요 — 다른 라우트(홈/검색/mypage 등)의 로딩 동작에 영향 줄 수 있어 신중히 설계할 것

### 개선하면 좋음 (미착수, 우선순위 낮음)
- 로그인 유도 잠금 카드 3곳(recommend/ai-recommend/상세) 중복 → 공용 컴포넌트화
- CTA 문구 불일치("무료로 시작하기" vs "회원가입" 등)
- 회사정보 입력 CTA 문구 3종("입력하기"/"보완하기"/"수정")
- 문의 창구 2개(고객센터 vs 의견보내기) 통합
- 터치 타겟 부족(상세 페이지 주 CTA 32px 등)
- 히어로 제목 고정폭, 상세 3분할 그리드 고정(모바일)
- 히어로 업종 칩이 바로 아래 필터와 중복
- 회원가입 폼 필드 단위 에러 메시지 없음
- `bid-detail-view.tsx` 전용 테스트 부재
- `public/`의 Next.js 기본 SVG 잔존물 정리
- CLAUDE.md "매칭 점수 항상 null" 설명 낡음(문서만 갱신하면 됨 — 실제로는 `VerdictBadge`로 이미 구현됨)
- `/recommend`의 `VerdictBadge`를 `BidCard`의 `tagSlot` 구조로 통합(`ai-reco-view.tsx`는 이미 `tagSlot`/`cornerSlot`을 쓰는데 `reco-view.tsx`만 카드 아래 별도 줄로 렌더 — 두 화면 방식이 다름)

---

## 3. PR #158 요약 (오늘 처리한 버그 묶음)

- `/bidbot` 정적 프리렌더 고착 해소(완전 해결 아님, 위 2번 참고)
- 로그아웃/세션확인중 `/mypage` 백지 → Topbar+스켈레톤+Footer로 교체
- `/mypage/account` 모바일 진입 경로 복구(UserMenu에 "계정 설정" 추가)
- `/recommend` 카드 그리드 배지 정렬(`BidCard`에 `h-full` 추가)
- `/guide` 페이지의 비활성 챗봇 기능 언급 제거(`BIDBOT_ENABLED` 연동)
- Open Graph/Twitter 메타태그 + `opengraph-image.tsx`(코드로 생성하는 OG 이미지) 추가
- 공고 목록 API fetch에 10초 타임아웃 추가(백엔드 응답 지연 시 빌드 무한대기 방지)

### ⚠️ 확인 필요: OG 이미지 한글 폰트 리스크
`opengraph-image.tsx`의 한글 렌더는 빌드 중 `fonts.googleapis.com` 네트워크 요청에 의존한다. egress가 막히면 **빌드는 성공하지만 한글이 두부(□□□□)로 깨진 이미지가 조용히 배포될 수 있음을 직접 재현해 확인했다.** 배포된 `/opengraph-image`는 로컬 검증본과 바이트 크기(31,804바이트)가 일치해 정상 렌더된 것으로 보이지만, **Threads·카카오톡 등 실제 링크 공유로 한 번 육안 확인 권장.** 근본 해결(한글 폰트 subset 파일을 저장소에 번들링)은 파일 다운로드가 필요해 별도 승인을 받아야 진행 가능 — 원하면 다음 세션에서 바로 처리 가능.

---

## 4. 로컬 개발 환경

```bash
# VPN(Tailscale)이 켜져 있으면 VPC 전체(10.0.0.0/16)가 서브넷 라우팅되어
# 터널 없이 바로 붙는다 — 아래 터널은 Tailscale이 꺼져 있을 때만 필요하다.
# 단, .env/.env.local이 localhost:5433/127.0.0.1:8004 같은 터널 전제 값으로
# 하드코딩돼 있어서, VPN이 멀쩡해도 이 값들 때문에 안 되는 것처럼 보일 수 있다
# — "안 된다"고 바로 터널부터 파지 말고 Tailscale 상태부터 확인할 것.

# RDS 터널 (Tailscale 없을 때)
ssh -i C:/Users/user/Desktop/PROJECTS/airflow.pem -N \
  -L 5433:bidmate-postgres.cv8i80gskzwz.ap-northeast-2.rds.amazonaws.com:5432 \
  ubuntu@54.116.198.166

# 로컬 백엔드
cd bidmate-backend && ./.venv/Scripts/python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8004

# 로컬 프론트
cd bidmate-frontend && npm run dev   # :3000, .env.local의 API_BASE_URL=http://127.0.0.1:8004
```

- 배스천 키는 `bidmate-api-dev.pem`이 아니라 **`airflow.pem`**. ssh가 "bad permissions"로 죽으면 `icacls`로 ACL 조여야 함.
- Turbopack `.next` 캐시가 가끔 낡은 청크를 물고 있을 수 있다 — 정상 소스인데 `ReferenceError` 등이 뜨면 새 브라우저 탭으로 먼저 확인하고(브라우저 캐시일 수 있음), 그래도 재현되면 `.next` 삭제 후 재기동.

---

## 5. 작업 방식 메모 (CLAUDE.md + 세션 중 확립된 것)

- 순서: 이슈 생성 → 이슈번호 브랜치 → `ui-producer`가 구현 → `design-reviewer`+`ui-qa` 병렬 검토 → 필요한 것만 골라 최대 1회 추가 수정 → 로컬 tsc/lint/test/build 독립 재검증 → 커밋·푸시 → (사용자 확인 후) PR 생성 → CI 통과 확인 → squash 머지·브랜치 삭제.
- 구현 지시 전엔 조사·계획 보고까지만, 지시 받으면 문제점/계획/기획이유/예측을 먼저 보고하고 확인받은 뒤 착수.
- PR 생성·머지는 매번 별도로 물어본 뒤 진행(자동 진행 금지).
- 배포는 GitHub Actions가 main push 시 자동으로 한다(별도 트리거 불필요, merge = 배포).
