# CLAUDE.md — bidding-agent-frontend

Claude Code가 이 레포에서 작업할 때 항상 따르는 규칙과 컨텍스트.

---

## 프로젝트 개요

조달청 나라장터 공공입찰 공고를 **회원사 조건에 맞춰 추천**하는 서비스의 **프론트엔드**.

- 백엔드/에이전트는 별도 레포(`final-pjt-supply/bidding-agent`)에서 개발. 이 레포는 화면만 담당.
- 전체 회원제. 핵심 화면:
  - **메인 홈**: 매칭 점수순 추천공고(카드 그리드) + 상단 "최근 등록된 공고" 가로 스크롤
  - **공고 상세**: 우리 회사 적합도 표 + 공고 정보 + 자격요건
  - **챗봇**: 플로팅(간단 Q&A) + 전용 페이지(대화 기록) — 구현 예정
- **매칭 점수는 에이전트가 나중에 채움** → 현재 항상 null, 화면엔 placeholder("매칭 —").
- 데이터는 `qual_status='merged'`인 공고만 노출(pending 미노출).

## 기술 스택

- **Next.js (App Router) + TypeScript**
- 상태관리 라이브러리 도입 금지(useState로 충분할 때까지)
- 공고 데이터는 백엔드 API에서 받는다(목업 없음 — `bids.json`은 실 API 전환 후 삭제됨, #203).
  `src/lib/data/`에 남은 것은 면허·인증·지역·직급 **마스터 데이터**(`masters.ts`)로, 이건 계속 쓴다.
- 타입: `@/lib/types` · 전처리 함수: `@/lib/format`

## 전처리 규칙 (표시용 가공은 프론트가)

- 원본 데이터는 건드리지 않고, 화면에 보여줄 때만 가공
- 업종 코드 → 한글: `categoryLabel` (cnstwk=공사 등)
- 긴 낙찰방법 → 카드 태그엔 앞부분만: `shortMethod` (상세는 원문 전체)
- 금액: `formatAmount` (배정예산 없으면 추정가격 대체)
- D-day: `computeDday` (마감일 null→"마감 미정", 1년 초과→"상시")

---

## Git Commit Convention (백엔드 레포와 통일)

형식: `Type : 설명` (한글)

**Commit Type**
- `Feat` — 새로운 기능 추가
- `Fix` — 버그 수정
- `Docs` — 문서 수정
- `Style` — 코드 포맷팅, 세미콜론 누락 등 (코드 변경 없음)
- `Refactor` — 코드 리팩토링
- `Test` — 테스트 코드 추가/수정
- `Chore` — 빌드 업무, 패키지 매니저 수정
- `Build` — 빌드 파일 수정
- `Ci` — CI 설정 파일 수정
- `Perf` — 성능 개선
- `Rename` — 파일/폴더명 수정
- `Remove` — 파일 삭제

예시:
```
Feat : 메인 홈 공고 카드 컴포넌트 추가
Fix : D-day 계산에서 마감일 null 처리
Docs : README 프로젝트 소개 추가
```

## Git Branch

1. 대문자 사용 안 함
2. 이음자는 `-` 사용
3. 이슈번호는 브랜치 맨 뒤에 붙임 → 예: `feat/main-home#12`

- `main` — 제품 출시 브랜치
- `develop` — 출시를 위해 개발하는 브랜치
- `feat/{기능명}` — 새 기능 개발
- `refactor/{기능명}` — 리팩토링
- `fix/{기능명}` — 버그 수정

예시: `feat/bid-detail#5`, `refactor/bid-card#9`

## Pull Request

1. 제목은 `[#이슈번호] 변경 사항` 구조
2. Issue와 연동
3. (혼자 작업이므로 담당자 표는 생략 가능)

예시: `[#2] 메인 홈 구현`, `[#5] 공고 상세 페이지 구현`

---

## 작업 방식

- 커밋은 의미 있는 한 덩어리가 끝날 때마다.
- 파괴적 명령(파일 대량 삭제 등) 전에 사용자에게 확인.
- 큰 작업은 단계로 나눠 진행하고, 각 단계 후 보고.

---

## UI/UX 에이전트 시스템

UI/UX 변경은 메인 Claude가 조율하고, 역할별 프로젝트 서브에이전트를 순서대로 사용한다.

### 역할

- `ux-researcher`: 구현 전 사용자 목표·정보 위계·중복 행동을 읽기 전용으로 분석
- `ui-producer`: 승인된 범위만 구현하는 유일한 파일 편집자
- `design-reviewer`: 구현 후 시각적 위계·CTA 중복·과잉 요소를 읽기 전용으로 검토
- `ui-qa`: 상태별 동작·API 의미·접근성·회귀를 읽기 전용으로 검증

### 필수 작업 순서

1. 메인 Claude가 사용자 요청과 변경 대상 화면을 확인한다.
2. `ux-researcher`에게 현재 화면과 구현을 분석시킨다.
3. 메인 Claude가 분석 결과에서 실제 구현 범위를 결정한다.
4. `ui-producer` 한 명만 파일을 수정한다.
5. 구현 후 `design-reviewer`와 `ui-qa`가 독립적으로 검토한다.
6. 메인 Claude가 두 검토 결과를 통합하고 필요한 수정만 선택한다.
7. 추가 수정은 `ui-producer`에게 한 번만 요청한다.
8. 관련 테스트, ESLint, 프로덕션 빌드, 실제 브라우저 검증 후 종료한다.

수정 반복은 원칙적으로 최대 2회다. 검토 결과를 전부 기계적으로 반영하지 말고 제품 목표와 변경 범위에 맞는 의견만 선택한다.

### 권한 원칙

- 파일을 수정하는 서브에이전트는 `ui-producer`뿐이다.
- 검토 에이전트는 파일을 수정하거나 커밋하지 않는다.
- 여러 제작 에이전트가 같은 파일을 동시에 수정하지 않는다.
- API 데이터의 의미를 확인하지 않고 오류로 단정하지 않는다.
- 백엔드 판정값과 비즈니스 로직을 디자인 판단으로 변경하지 않는다.
- 사용자 변경사항과 범위 밖 코드는 보존한다.

### 제품 디자인 원칙

BidFriend는 공공입찰 담당자가 공고를 빠르게 검토하고 참여 여부와 다음 행동을 결정하도록 돕는 B2B 업무 서비스다. 화려함보다 다음 기준을 우선한다.

1. 정보의 정확성과 신뢰
2. 의사결정 속도
3. 명확한 정보 위계
4. 중복되지 않는 행동
5. 기존 디자인 시스템과의 일관성

- 한 영역의 주요 CTA는 원칙적으로 하나다.
- 같은 목적지나 기능을 가진 버튼을 다른 문구로 반복하지 않는다.
- 새 요소를 추가하기 전에 기존 요소와 통합하거나 삭제할 수 있는지 확인한다.
- 상태값, 설명, 사용자 행동을 시각적으로 구분한다.
- 중요한 정보는 3초 안에 파악할 수 있어야 한다.
- 카드·배지·버튼·테두리를 불필요하게 늘리지 않는다.
- 분석 결과 뒤에는 필요한 다음 행동을 제공하되 기존 행동과 중복하지 않는다.
- 화면의 정보 요약은 상세 정보 탐색을 빠르게 할 때만 허용한다.

### 구현 후 필수 질문

- 새로 추가한 요소 중 삭제해도 되는 것은 무엇인가?
- 동일한 기능이나 목적지를 가진 버튼이 두 개 이상 있는가?
- 가장 강조된 요소가 사용자의 가장 중요한 행동인가?
- UI가 API 데이터에 근거하지 않은 추론을 하고 있는가?
- 변경 전보다 사용자의 판단이 실제로 빨라졌는가?
- 로딩·오류·빈 값·로그인 상태에서도 문구와 행동이 정확한가?

## 외부 플러그인과의 관계 (ponytail)

ponytail 플러그인을 쓰는 경우에만 해당한다. 설치하지 않았다면 이 절은 무시해도 된다.

ponytail의 지향("가장 단순한 해법", "추가보다 삭제", "요청하지 않은 추상화 금지")은
위 **제품 디자인 원칙**과 같은 방향이므로 기본적으로 함께 써도 된다.
겹치지 않는 부분만 아래로 정리한다.

**우선순위: 사용자 지시 > 이 문서 > ponytail.**

- **ponytail은 "무엇을 만들지"만 지배한다.** 작업 절차와 보고 방식은 이 문서가 우선한다 —
  이슈→브랜치→서브에이전트 순서→PR 흐름, 단계별 보고, 착수 전 계획 보고는 그대로 지킨다.
  ponytail의 "설명은 최대 3줄" 규칙은 여기 해당하지 않는다(ponytail 자신도
  "사용자가 요청한 설명은 부채가 아니다", "말하는 방식은 지배하지 않는다"고 명시한다).
- **레벨은 `full`까지만 쓴다.** `ultra`는 요구사항 자체에 이의를 제기하는 모드라
  정해진 작업 순서와 충돌한다. 쓰지 않는다.
- **`ponytail:` 주석은 쓰지 않는다.** 의도적으로 단순하게 간 지점의 근거는
  기존 방식대로 일반 주석에 남긴다. `/ponytail-debt`도 쓰지 않는다.
- **검토 에이전트의 판단 기준은 이 문서다.** ponytail은 서브에이전트에도 주입되지만,
  `ux-researcher`·`design-reviewer`·`ui-qa`의 기준은 위 제품 디자인 원칙과 필수 질문이다.
  구현 범위를 줄이는 판단은 `ui-producer`에게만 적용된다.
- ponytail이 "생략하면 안 된다"고 명시한 항목(입력 검증, 에러 처리, 접근성 기본, 보안)은
  `ui-qa` 검증 항목과 겹친다. 둘 중 어느 쪽으로도 줄이지 않는다.

## 설계 문서 (Notion, 프론트엔드 페이지 하위)

- 메인 홈 — 화면 정의서
- 공고 상세 — 화면 정의서
- 서빙 API 응답 계약 (확정본)
- (데이터 스키마) bid_table — 데이터스키마 페이지 하위

화면·데이터 관련 결정은 위 문서를 기준으로 함.
